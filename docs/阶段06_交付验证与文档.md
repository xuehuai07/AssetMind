# 阶段 06：交付验证与文档

## 目标

完成项目交付材料、验证闭环和风险说明，确保评审者可以快速运行、理解和追问。

## 开发内容

- 完成 README。
- 回填总体设计文档和阶段文档实现结果。
- 执行 lint、build、API smoke test 和页面访问验证。
- 整理未完成事项、生产化限制和后续迭代建议。

## 阶段结果

- README 覆盖项目介绍、本地启动、技术栈、API、数据结构、检索实现、本地 JSON 取舍、DeepSeek 接入、真实向量库改造、多租户改造和 ToB 风险。
- 总体设计文档和阶段文档具备开发历程记录。
- 验证过程使用测试 JSON 文件，避免污染正式 seed 数据。

## 验证

- `npm run lint` 通过。
- `npm run build` 通过。
- `git diff --check` 通过。
- 首页返回 200。
- 资产、搜索、问答和上传 API 的关键路径通过 smoke test。

## 风险记录

- `npm audit --audit-level=moderate` 报告 Next 依赖链中的 PostCSS moderate 漏洞。
- 自动修复需要破坏性 `npm audit fix --force`，本阶段未执行。
